// src/controller/atoms/middleware/authorize.ts
// Authorization middleware — stub until role system is implemented

import { Elysia } from "elysia";

/**
 * Stub plugin — pass-through until roles are defined.
 * Add .guard() logic here when permissions are implemented.
 */
export const authorizePlugin = new Elysia();
